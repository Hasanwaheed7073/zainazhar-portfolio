# SEO / GEO / AEO Fixes — August 2, 2026

Response to the full audit of zainazhar.vercel.app (audit dated Aug 2, 2026, scores SEO 7 / GEO 7 / AEO 6).

## Important context

The audit was run against the DEPLOYED site, which is behind this codebase. Several "critical" findings were already fixed in code but never deployed: the 3,400+ interview count, the single canonical LinkedIn URL (linkedin.com/in/zainazhar), SSR'd real stat values (no more "0+"), page-specific twitter titles, and JSON-LD (Person, ProfessionalService, FAQPage, Service, Article, BreadcrumbList). **Deploying this build resolves those on its own.**

## Fixed in this pass (code)

1. **Comparison tables (AEO quick win)** — pricing-model table added to the cost post; coach-vs-reverse-recruiter table added to the comparison post; evaluation table added to the best-services post. New `table` block type in `lib/content.ts`, rendered in `app/blog/[slug]/page.tsx`.
2. **Visible published/updated dates** — shown under every post H1 and on the blog index, with `<time>` tags. `updated` field added to the Post type.
3. **Deepened the two highest-intent posts** — "how much does a reverse recruiter cost" and "best reverse recruiter services" substantially expanded with tables, checklists, question-phrased H2s, and cited sources. Both marked Updated 2026-08-02.
4. **External citations (GEO)** — BLS Table A-12 (median unemployment duration 11.6 weeks, May 2026 — verified against bls.gov on 2026-08-02) cited in the cost and how-long posts; Harvard Business School "Hidden Workers" (2021) cited in the ATS and best-services posts. New `cite` block type renders the claim with a linked source. NOTE: hbs.edu timed out during verification — click-check the two HBS PDF links once before deploy.
5. **Richer Article schema** — dateModified from `updated`, author sameAs (canonical LinkedIn), publisher, image, and Speakable markup on all posts.
6. **Blog index schema** — Blog/BlogPosting JSON-LD listing all 8 posts with dates.
7. **Person schema sameAs** — added x.com/zainazhar alongside LinkedIn (matches existing twitter:creator).
8. **Sitemap accuracy** — post URLs now use real published/updated dates for lastModified instead of build time.
9. **Duplicate content bloat** — tracker lists were rendered twice (mobile accordion + desktop). New `ResponsiveDetails` component renders once: SSR open for crawlers, collapses on mobile after hydration.
10. **Thin specialty pages** — 2 role-specific FAQs added to /reverse-recruiter/executive and 2 to /reverse-recruiter/software-engineering (also feeds their FAQPage schema).

## Still requires action outside this codebase

- **Deploy this build** — most audit criticals clear on deploy.
- **Custom domain (audit critical #2)** — buy zainazhar.com, point it at Vercel, update `metadataBase`/canonicals/JSON-LD URLs from zainazhar.vercel.app, and set 301 redirects. Then switch zeecareers07@gmail.com to a branded address (e.g., zain@zainazhar.com) in Footer.tsx, Contact.tsx, and page.tsx JSON-LD.
- **Google Search Console** — set up now, before any domain migration.
- **Verify schema** after deploy at search.google.com/test/rich-results and validator.schema.org.
- **Core Web Vitals** — run pagespeed.web.dev after deploy (not assessable from the audit crawl).
- Only remaining proof gap: software-engineering has 1 tracker, executive has 2 — add more trackers to `lib/content.ts` when new campaigns can be shared.
